//* Literals

//* JavaScript defines two types of values: Fixed values and variable vales.
//* Fixed values: literals (hardcoded)
//* Variable values: variables

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString()); //method
console.log(days.length); //property

let tired = true;

let soup = {
  a: 'chicken noodle',
  b: 'tomato',
  c: 'beef and barley'
}
console.log(soup.c); // dot notation
console.log(typeof soup.c);

let car = 'Honda';

let dec = 10.2